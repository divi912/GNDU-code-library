# Document files

Place contributed documents in this directory. Each filename must exactly match the
material `id` in `src/data/materials.json`, followed by the extension associated with
its `type`. See `CONTRIBUTING.md` at the project root for the complete contribution
process.
